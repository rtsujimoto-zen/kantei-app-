services:
  - type: web
    name: sanmei-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn webapp.backend.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
